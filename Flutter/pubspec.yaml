name: klinik_app
description: Sistem Pendaftaran Pasien Klinik - Flutter Mobile App (REST API Client)
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  flutter_tts: ^4.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
  # assets:
  #   - assets/1.jpeg

  # Custom font (nonaktif dulu, filenya belum ada — aktifkan lagi kalau sudah ditambahkan)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/font/Poppins-Regular.ttf
  #       - asset: assets/font/Poppins-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/font/Poppins-Bold.ttf
  #         weight: 700
